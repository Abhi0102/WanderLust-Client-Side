import { NumberStylePipe } from './number-style.pipe';

describe('NumberStylePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberStylePipe();
    expect(pipe).toBeTruthy();
  });
});
