import { BookingSortPipe } from './booking-sort.pipe';

describe('BookingSortPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingSortPipe();
    expect(pipe).toBeTruthy();
  });
});
